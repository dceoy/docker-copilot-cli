---
services:
  copilot-cli:
    container_name: copilot-cli
    image: dceoy/copilot-cli:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: cli
      args:
        USER_NAME: copilot
        USER_UID: '1001'
        USER_GID: '1001'
    environment:
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
    tty: true
    stdin_open: true
    volumes:
      - .:/workspace
      - copilot-data:/home/copilot/.copilot
      - config-data:/home/copilot/.config
    working_dir: /workspace
    post_start:
      - command: bash -c '[[ "$(stat -c "%U:%G" /home/copilot/.copilot)" != "root:root" ]] || chown copilot:copilot /home/copilot/.copilot'
        user: root
      - command: bash -c '[[ "$(stat -c "%U:%G" /home/copilot/.config)" != "root:root" ]] || chown copilot:copilot /home/copilot/.config'
        user: root
    entrypoint:
      - /usr/bin/zsh
    command:
      - -lc
      - 'copilot --yolo'
volumes:
  copilot-data:
  config-data:
